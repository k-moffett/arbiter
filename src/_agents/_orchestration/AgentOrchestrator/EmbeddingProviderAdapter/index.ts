/**
 * Embedding Provider Adapter
 *
 * Adapts Ollama's single-text embed() to batch interface.
 */

export { EmbeddingProviderAdapterImplementation as EmbeddingProviderAdapter } from './EmbeddingProviderAdapterImplementation';
export type { EmbeddingProvider } from './types';
