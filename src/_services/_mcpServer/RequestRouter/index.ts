/**
 * RequestRouter
 *
 * Exports the RequestRouter implementation and related types.
 */

export type { ResourceHandler, ToolHandler } from './interfaces';

export { RequestRouterImplementation as RequestRouter } from './RequestRouterImplementation';
