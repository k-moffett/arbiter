/**
 * BaseLogger Enums
 *
 * Enum definitions for logging operations.
 */

/**
 * Log levels supported by BaseLogger
 */
export enum LogLevel {
  DEBUG = 'debug',
  ERROR = 'error',
  FATAL = 'fatal',
  INFO = 'info',
  WARN = 'warn',
}
